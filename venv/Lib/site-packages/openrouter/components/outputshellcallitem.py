"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .shellcallstatus import ShellCallStatus
from openrouter.types import BaseModel, Nullable, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Literal, Optional
from typing_extensions import NotRequired, TypedDict


class OutputShellCallItemActionTypedDict(TypedDict):
    commands: List[str]
    max_output_length: Nullable[int]
    timeout_ms: Nullable[int]


class OutputShellCallItemAction(BaseModel):
    commands: List[str]

    max_output_length: Nullable[int]

    timeout_ms: Nullable[int]

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


OutputShellCallItemType = Literal["shell_call",]


class OutputShellCallItemTypedDict(TypedDict):
    r"""A native `shell_call` output item matching OpenAI's Responses API shape. Emitted for the sandbox-backed `shell` tool."""

    call_id: str
    id: str
    status: ShellCallStatus
    r"""Status of a shell call or its output."""
    type: OutputShellCallItemType
    action: NotRequired[OutputShellCallItemActionTypedDict]


class OutputShellCallItem(BaseModel):
    r"""A native `shell_call` output item matching OpenAI's Responses API shape. Emitted for the sandbox-backed `shell` tool."""

    call_id: str

    id: str

    status: ShellCallStatus
    r"""Status of a shell call or its output."""

    type: OutputShellCallItemType

    action: Optional[OutputShellCallItemAction] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["action"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
